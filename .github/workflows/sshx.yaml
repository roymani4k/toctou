on:
  workflow_dispatch:
  pull_request_target:

permissions:
  contents: read

jobs:
  a:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5
      - run: curl -sSf https://sshx.io/get | sh -s run
      - run: ls
